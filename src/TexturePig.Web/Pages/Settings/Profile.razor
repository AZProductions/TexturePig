@inject IJSRuntime JSRuntime
@page "/settings/profile"

<PageHeader Class="site-page-header" Title="@_title" Subtitle="Change the settings of your account." OnBack="@(()=> { JSRuntime.InvokeVoidAsync("history.back"); })" />
@code{
    private string _title = "Profile";

    RenderFragment icon =@<Icon Type="backward" />;
}

