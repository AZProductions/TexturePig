namespace TexturePig.Driver
{
    public class Provider
    {
        public Provider(string urlLocation)
        {
            UrlLocation = urlLocation;

        }

        public string UrlLocation { get; }
    }
}